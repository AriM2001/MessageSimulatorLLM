Instructions To Run:
Prerequesties: 
  Be sure to have local LLM running on local machine (providers.py include ollama, openai, and anthropic(Claude) JSON for using the model) 
  Have required python libraries installed(SimPy, sklearn, matplotlib)
1. Open the Message LLM IRL.ipynb found in the LLM Simulator folder.
2. Run the first cell (runs other python files) 
3. Run every cell that sequentially follows for the results

** I end up keeping it as a notebook file since it made it easier for me to keep working on the file and by the end I determined the ease of repeating the simulation in it made me keep it. 
